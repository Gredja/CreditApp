namespace CreditApp.Helpers
{
    public enum Currency
    {
        USD,
        EUR,
        BYN
    }

    public enum Roles
    {
        Admin,
        User
    }

    public enum AppFolder
    {
        Default
    }
}
